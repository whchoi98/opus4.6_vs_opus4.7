# Runbook: [Operation Name]

**Purpose:** One-sentence summary of what this procedure accomplishes.

**When to run:** Trigger conditions (schedule, event, user request).

**Estimated time:** N minutes

**Estimated cost:** $X

**Prerequisites:**
- `.env.local` with valid AWS credentials
- `pip install --user -r requirements.txt`
- `python3 -m pytest tests/` passing

## Procedure

### Step 1: [Name]

```bash
<command>
```

**Expected output:** `<what to verify>`

**If it fails:** `<recovery steps>`

### Step 2: [Name]

...

## Verification

- [ ] `<specific file/output to check>`
- [ ] `<specific metric to validate>`

## Rollback

If something goes wrong:

```bash
<rollback commands, if applicable>
```

## References

- Related code: `<file>`
- Related finding: `docs/superpowers/findings/<file>.md`
